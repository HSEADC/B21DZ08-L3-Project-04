<div class="W_Body">
    <%= form_with(model: [:admin, post]) do |form| %>
      <% if post.errors.any? %>
        <div style="color: red">
          <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

          <ul>
            <% post.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <%= form.label :title, "Заголовок", style: "display: block", class: "A_Heading3" %>
        <%= form.text_field :title, class: "A_Input"%>
      </div>

      <div>
        <%= form.label :text, "Основной текст", style: "display: block", class: "A_Heading3" %>
        <%= form.rich_text_area :text , class: "A_Input"%>
      </div>

      <div>
      <%= form.submit "Опубликовать", class:"A_Button Blue" %>
      </div>
    <% end %>
</div>