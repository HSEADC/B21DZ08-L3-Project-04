<%= form_with(model: [:admin, @subscription]) do |form| %>
  <% if @subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="Form">
      <%= form.text_field :email, class: "A_Input", placeholder: "@bubochka" %>
    <div>
      <%= form.submit "отправить", class: "A_InputButton" %>
    </div>
  </div>
<% end %>